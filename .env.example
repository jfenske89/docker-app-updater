# Copy to ./.env (gitignored) and fill in for your VPS.
# Used by scripts/deploy.sh.

VPS_HOST=user@your-vps-hostname
VPS_PATH=/opt/docker-app-updater/docker-app-updater

# Optional: extra scp flags, e.g. a non-default identity file or port.
# VPS_SSH_OPTS=-i ~/.ssh/vps_key -P 2222
